# have to create the config/default.json and config/production.json files in this dockerfile
# 	pass in the secrets via github pipeline secrets
# e.g. config/production.json
# {
# 	"VARS": {
# 		"postgres_password": "secret-pg-password",
# 		"jwt_key": "secret-jwt-key"
# 	}
# }